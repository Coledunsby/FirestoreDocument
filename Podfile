inhibit_all_warnings!
use_frameworks!

platform :ios, '8.0'

target 'FirestoreDocument' do

    podspec

    pod 'Sourcery'
    pod 'SwiftLint'

    target 'FirestoreDocumentTests' do
        inherit! :search_paths
        pod 'Nimble'
    end

end
